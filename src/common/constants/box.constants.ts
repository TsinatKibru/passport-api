/**
 * Box-related constants
 * 
 * Single source of truth for box configuration values.
 */

/**
 * Default capacity for movable boxes.
 * All boxes are standardized at 10 passports unless explicitly configured otherwise.
 */
export const DEFAULT_BOX_CAPACITY = 10;
